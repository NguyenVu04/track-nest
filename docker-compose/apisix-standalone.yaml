routes:
  - uri: /*
    enable_websocket: true
    plugins:
      openid-connect:
        client_id: "apisix-client"
        client_secret: "b6b0x6mSibBpYdtqq5i5XPfnYYMgZToC"
        discovery: "http://keycloak:8080/realms/tracknest/.well-known/openid-configuration"
        introspection_endpoint: "http://keycloak:8080/realms/tracknest/protocol/openid-connect/token/introspect"
        scope: "openid profile"
        realm: "tracknest"
        set_userinfo_header: true
        bearer_only: true
    upstream:
      type: roundrobin
      nodes:
        "host.docker.internal:18080": 1
#END