apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-controller
spec:
  ingressClassName: apisix
  rules:
    - http:
        paths:
          - backend:
              service:
                name: emergency-ops
                port:
                  number: 8080
            path: /emergency-ops
            pathType: Prefix