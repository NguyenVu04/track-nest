apiVersion: v1
kind: Service
metadata:
  name: nginx
spec:
  type: LoadBalancer   # or NodePort / ClusterIP + Ingress
  selector:
    app: nginx
  ports:
    - name: http
      port: 80
      targetPort: {{ .Values.nginx.port }}