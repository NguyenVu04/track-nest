application:
  name: tracknest
  namespace: tracknest

redis:
  enabled: true
  architecture: replication
  auth:
    enabled: true
    password: "redis-admin"
  master:
    count: 1
  replica:
    replicaCount: 1
    kind: StatefulSet

kafka:

apisix-ingress-controller:

apisix:

elasticsearch:

milvus:

minio:

vault: